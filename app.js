const STORAGE_KEY="music-chat-lab.chats.v1",ACTIVE_KEY="music-chat-lab.active-chat.v1",SETTINGS_KEY="music-chat-lab.api-settings.v1",COST_KEY="music-chat-lab.cost-control.v1";
const providerModels={anthropic:[{id:"claude-sonnet-5",label:"Claude Sonnet 5"},{id:"claude-opus-5",label:"Claude Opus 5"},{id:"claude-sonnet-4-6",label:"Claude Sonnet 4.6"}],openai:[{id:"gpt-5.6-sol",label:"GPT-5.6 Sol"},{id:"gpt-5.6-terra",label:"GPT-5.6 Terra"},{id:"gpt-5.6-luna",label:"GPT-5.6 Luna"}],google:[{id:"gemini-3.8-flash",label:"Gemini 3.8 Flash"},{id:"gemini-3.7-flash",label:"Gemini 3.7 Flash"},{id:"gemini-3.1-pro-preview",label:"Gemini 3.1 Pro Preview"}]};
let chats=loadChats(),activeChatId=localStorage.getItem(ACTIVE_KEY)||null,pendingFiles=[],sending=false;window.pendingFiles=pendingFiles;
const sidebar=document.getElementById("sidebar"),chatList=document.getElementById("chatList"),messages=document.getElementById("messages"),welcome=document.getElementById("welcome"),currentChatTitle=document.getElementById("currentChatTitle"),providerSelect=document.getElementById("providerSelect"),modelSelect=document.getElementById("modelSelect"),newChatButton=document.getElementById("newChatButton"),menuButton=document.getElementById("menuButton"),messageInput=document.getElementById("messageInput"),sendButton=document.getElementById("sendButton"),fileInput=document.getElementById("fileInput"),attachments=document.getElementById("attachments"),composerNote=document.getElementById("composerNote"),settingsButton=document.getElementById("settingsButton"),topSettingsButton=document.getElementById("topSettingsButton"),settingsDialog=document.getElementById("settingsDialog"),settingsForm=document.getElementById("settingsForm"),anthropicKeyInput=document.getElementById("anthropicKey"),openaiKeyInput=document.getElementById("openaiKey"),googleKeyInput=document.getElementById("googleKey"),clearKeysButton=document.getElementById("clearKeysButton");
function loadChats(){try{return JSON.parse(localStorage.getItem(STORAGE_KEY))||[]}catch{return[]}}function loadSettings(){try{return JSON.parse(localStorage.getItem(SETTINGS_KEY))||{}}catch{return{}}}function saveChats(){localStorage.setItem(STORAGE_KEY,JSON.stringify(chats))}function saveSettings(s){localStorage.setItem(SETTINGS_KEY,JSON.stringify(s))}function makeId(){return crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random()}`}function currentChat(){return chats.find(c=>c.id===activeChatId)||null}
function createChat(){const c={id:makeId(),title:"Neuer Chat",createdAt:Date.now(),updatedAt:Date.now(),provider:providerSelect.value,model:modelSelect.value,messages:[]};chats.unshift(c);activeChatId=c.id;localStorage.setItem(ACTIVE_KEY,activeChatId);saveChats();pendingFiles=[];window.pendingFiles=pendingFiles;renderAll();messageInput.focus()}
function modelsFor(provider){return window.MCLModelCatalog?.list?.(provider)||providerModels[provider]||[]}function updateModels(preferredModel=null){const ms=modelsFor(providerSelect.value);modelSelect.innerHTML="";ms.forEach(m=>{const o=document.createElement("option");o.value=m.id;o.textContent=m.label;modelSelect.appendChild(o)});if(preferredModel&&ms.some(m=>m.id===preferredModel))modelSelect.value=preferredModel}function providerLabel(p){return{anthropic:"Claude",openai:"OpenAI",google:"Gemini"}[p]||p}function modelLabel(p,m){return modelsFor(p).find(x=>x.id===m)?.label||m}
function renderChatList(){chatList.innerHTML="";[...chats].sort((a,b)=>b.updatedAt-a.updatedAt).forEach(c=>{const b=document.createElement("button");b.className=`chat-item${c.id===activeChatId?" active":""}`;const d=new Date(c.updatedAt).toLocaleString("de-DE",{day:"2-digit",month:"2-digit",hour:"2-digit",minute:"2-digit"});b.innerHTML='<div class="chat-item-title"></div><div class="chat-item-meta"></div>';b.querySelector(".chat-item-title").textContent=c.title;b.querySelector(".chat-item-meta").textContent=`${providerLabel(c.provider)} · ${d}`;b.addEventListener("click",()=>{activeChatId=c.id;localStorage.setItem(ACTIVE_KEY,activeChatId);pendingFiles=[];window.pendingFiles=pendingFiles;sidebar.classList.remove("open");renderAll()});chatList.appendChild(b)})}
function visibleText(t){return String(t||'').replace(/<MCL_(?:ACTION|NEED|MEMORY|CONCEPT)>[\s\S]*?<\/MCL_(?:ACTION|NEED|MEMORY|CONCEPT)>/gi,'').replace(/\n*\[MCL-(?:VORSCHLAG|QUELLEN):[^\]]*\]\s*/gi,'').trim()}
function renderMessages(){const c=currentChat();messages.innerHTML="";if(!c||!c.messages.length){messages.appendChild(welcome.cloneNode(true));return}c.messages.forEach(m=>{const r=document.createElement("div");r.className=`message-row ${m.role}${m.isError?" error":""}`;const meta=document.createElement("div");meta.className="message-meta";meta.textContent=m.role==="user"?"Du":m.isError?"Music Chat Lab":`${providerLabel(m.provider)} · ${modelLabel(m.provider,m.model)}`;const b=document.createElement("div");b.className=`message-bubble${m.thinking?" thinking":""}`;b.textContent=m.displayText??visibleText(m.text);if(m.files?.length)m.files.forEach(f=>{const chip=document.createElement("div");chip.className="file-chip";chip.textContent=`📎 ${f.name} · ${formatBytes(f.size)}${f.summary?` · ${f.summary}`:""}`;b.appendChild(document.createElement("br"));b.appendChild(chip)});r.append(meta,b);messages.appendChild(r)});messages.scrollTop=messages.scrollHeight}
function renderPendingFiles(){attachments.innerHTML="";pendingFiles.forEach((f,i)=>{const p=document.createElement("div"),n=document.createElement("span"),x=document.createElement("button");p.className="attachment-pill";n.textContent=`${f.name} · ${formatBytes(f.size)}`;x.textContent="×";x.addEventListener("click",()=>{pendingFiles.splice(i,1);window.pendingFiles=pendingFiles;renderPendingFiles()});p.append(n,x);attachments.appendChild(p)});window.pendingFiles=pendingFiles;window.dispatchEvent(new CustomEvent('mcl-pending-files-rendered',{detail:{files:[...pendingFiles]}}))}
function formatBytes(b){if(b<1024)return`${b} B`;if(b<1048576)return`${(b/1024).toFixed(1)} KB`;return`${(b/1048576).toFixed(1)} MB`}function renderCost(){window.MCLUsageRender?.()}function renderAll(){let c=currentChat();if(!c&&chats.length){activeChatId=chats[0].id;localStorage.setItem(ACTIVE_KEY,activeChatId);c=currentChat()}if(c){providerSelect.value=c.provider;updateModels(c.model);currentChatTitle.textContent=c.title}else{updateModels();currentChatTitle.textContent="Neuer Chat"}sendButton.disabled=sending;renderChatList();renderMessages();renderPendingFiles();renderCost()}
function apiKeyFor(p){const s=loadSettings();return{anthropic:s.anthropicKey,openai:s.openaiKey,google:s.googleKey}[p]||""}function openSettings(){const s=loadSettings();anthropicKeyInput.value=s.anthropicKey||"";openaiKeyInput.value=s.openaiKey||"";googleKeyInput.value=s.googleKey||"";settingsDialog.showModal()}
const SCORE_BLOCK_RE=/\[MCL-ENGINE14-SCORE name=(?:"(?:[^"\\]|\\.)*")\]\n[\s\S]*?\n\[\/MCL-ENGINE14-SCORE\]/g,WORKSPACE_BLOCK_RE=/\n*--- MUSIKALISCHER ARBEITSTISCH ---[\s\S]*?--- ENDE MUSIKALISCHER ARBEITSTISCH ---\n*/g;
function scoreSummaryForApi(text){let s=String(text||'').trim();const fence=s.match(/```(?:json)?\s*([\s\S]*?)\s*```/i);if(fence)s=fence[1].trim();const a=s.indexOf('{'),b=s.lastIndexOf('}');if(a<0||b<=a)return null;try{const x=JSON.parse(s.slice(a,b+1));if(!x||!Array.isArray(x.tr)||!x.tr.some(t=>Array.isArray(t.nt)))return null;return`Komposition erzeugt: ${x.ti||'Unbenannt'}. ${x.sm||''}`.trim()}catch{return null}}
function compactApiText(text,role){const summary=role==='assistant'?scoreSummaryForApi(text):null;if(summary)return summary;return String(text||'').replace(WORKSPACE_BLOCK_RE,'\n').replace(SCORE_BLOCK_RE,'').replace(/\n\n--- DATEIANHÄNGE ---\n?/g,'\n').replace(/\n{3,}/g,'\n\n').trim()}function normalizedMessages(c){return c.messages.filter(m=>!m.isError&&!m.thinking&&(m.role==="user"||m.role==="assistant")).map(m=>{let text=compactApiText(m.text||'',m.role);if(m.attachmentContext)text=`${text}${m.attachmentContext}`.trim();return{role:m.role,text}})}
async function prepareAttachments(files){const metadata=[],blocks=[];for(const file of files){let processed;try{processed=await window.MusicFileProcessing.fileToModelText(file)}catch(e){processed={kind:"error",summary:"Lesefehler",text:`[Datei ${file.name} konnte nicht verarbeitet werden: ${e?.message||e}]`}}metadata.push({name:file.name,size:file.size,type:file.type,kind:processed.kind,summary:processed.summary});blocks.push(processed.text)}return{metadata,modelText:blocks.length?`\n\n--- DATEIANHÄNGE ---\n${blocks.join("\n\n---\n")}`:""}}
function requestSpec(c,key,providedSlots=[]){const mode=window.MCLRequestMode==='compose'?'compose':'chat',idea=window.MCLExplicitModeV200?.currentIdea?.()||'',turn=window.MCLSessionCore.buildTurn({messages:normalizedMessages(c),mode,idea,providedSlots});return{provider:c.provider,model:c.model,apiKey:key,mode,idea,messages:turn.messages,system:turn.system,turnId:window.MCLUsageActiveTurn?.()||null}}
async function requestAssistant(c){const key=apiKeyFor(c.provider);if(!key)throw new Error(`Für ${providerLabel(c.provider)} ist noch kein API-Schlüssel eingetragen.`);if(!window.MCLSessionCore||!window.MCLRequestRuntime)throw new Error('Die neue Request-Architektur ist nicht vollständig geladen.');const base=requestSpec(c,key);if(base.mode!=='compose'){let result=await window.MCLRequestRuntime.run(base);if(result.need?.slots?.length)result=await window.MCLRequestRuntime.run(requestSpec(c,key,result.need.slots));return result}const userMessage=[...(c.messages||[])].reverse().find(m=>m.role==='user')||null,userText=[...base.messages].reverse().find(m=>m.role==='user')?.text||'',forceNew=window.MCLCompositionIntent?.requiresNew?.({text:userText,message:userMessage})===true;const draftSystem='Du bist ausschließlich Komponist. Komponiere musikalisch frei und eigenständig. Konzentriere dich auf musikalische Gestalt, Verlauf, Stimmen, Rhythmus, Harmonik, Artikulation und Charakter. Denke noch NICHT an MIDI-Codierung, Beat-Werte, MCL_ACTION oder ein technisches Ausgabeformat. Schreibe einen vollständigen, konkret ausnotierbaren musikalischen Entwurf. Gib in der ersten Zeile einen kurzen passenden Werktitel als „Titel: …“ an. Mache keine Erläuterung über deine Arbeitsweise.';composerNote.textContent='KI komponiert musikalischen Entwurf …';const draft=await window.MCLRequestRuntime.run({...base,mode:'chat',system:draftSystem,messages:[{role:'user',text:userText}]});const musicalDraft=String(draft.visible||draft.text||'').trim();if(!musicalDraft)throw new Error('Die musikalische Kompositionsstufe lieferte keinen Entwurf.');composerNote.textContent='MIDI wird werkgetreu erzeugt …';const translateSystem=`Du bist ausschließlich Notations- und MIDI-Übersetzer. Übertrage den bereits fertigen musikalischen Entwurf so vollständig und werkgetreu wie möglich in Music Chat Labs MIDI-Aktionsformat. Komponiere NICHT neu, vereinfache NICHT, regularisiere NICHT den Rhythmus und ersetze keine ungewöhnlichen musikalischen Entscheidungen durch Standards. Bewahre insbesondere rhythmische Vielfalt, Pausen, Stimmführung, Phrasierung, Artikulation und Dynamik. Die endgültige Antwort muss genau eine gültige <MCL_ACTION> enthalten. Nutze NEW_SCORE für neue Musik; bei ausdrücklich verlangter Bearbeitung vorhandenen Materials die passende PATCH/REPLACE_SCORE/MERGE-Aktion. Notenformat nt=[StartBeat,Dauer,Pitch,Velocity,Staff,Gate], Controller ct=[Beat,CC,Wert]. Erhalte auch den zeitlichen Verlauf des Entwurfs: score.bpm ist das Anfangstempo; optionale Tempowechsel stehen als tm=[[Beat,BPM],...]. score.ts ist die Anfangstaktart; optionale Taktwechsel stehen als tsm=[[Beat,Zähler,Nenner],...]. Übernimm ausdrücklich notierte Tempo- und Taktwechsel vollständig und setze Dynamik und Artikulation werkgetreu mit Velocity, Gate und – soweit musikalisch sinnvoll – allgemeinen MIDI-Controllern um. Erfinde keine instrumentenspezifischen Keyswitches oder SWAM-Steuerungen. Antworte ohne zusätzliche musikalische Neuschöpfung.`;const freshRule=forceNew?'\n\nVERBINDLICHE AKTIONSREGEL FÜR DIESEN ZUG: Dies ist eine eindeutige Neukomposition ohne bereitgestelltes Ausgangsmaterial. Die Aktion MUSS type="NEW_SCORE" sein. baseSlot ist verboten. PATCH, REPLACE_SCORE und MERGE sind in diesem Zug unzulässig. Ein früherer Bearbeitungsmodus oder alter Arbeitstischzustand darf nicht übernommen werden.':'';const translateUser=`URSPRÜNGLICHER AUFTRAG:\n${userText}\n\nFERTIGER MUSIKALISCHER ENTWURF:\n${musicalDraft}`;let translated=await window.MCLRequestRuntime.run({...base,system:translateSystem+freshRule,messages:[{role:'user',text:translateUser}]});if(forceNew&&!window.MCLCompositionIntent?.accepts?.(translated.action,true)){const retrySystem=translateSystem+freshRule+'\nDie vorige technische Übersetzung verletzte diese Aktionsregel. Korrigiere ausschließlich das Ausgabeformat auf eine vollständige NEW_SCORE-Aktion; verwende keinen baseSlot.';translated=await window.MCLRequestRuntime.run({...base,system:retrySystem,messages:[{role:'user',text:translateUser}]});if(!window.MCLCompositionIntent?.accepts?.(translated.action,true))throw new Error('Neukomposition wurde nicht als NEW_SCORE geliefert; ein alter PATCH-Zustand wurde nicht übernommen.')}return translated}
async function sendMessage(){if(sending)return;const text=messageInput.value.trim();if(!text&&!pendingFiles.length)return;const p=providerSelect.value;if(!apiKeyFor(p)){composerNote.textContent=`Bitte zuerst den API-Schlüssel für ${providerLabel(p)} eintragen.`;openSettings();return}if(!currentChat())createChat();const c=currentChat();c.provider=providerSelect.value;c.model=modelSelect.value;sending=true;sendButton.disabled=true;composerNote.textContent=pendingFiles.length?"Dateien werden lokal analysiert …":`${providerLabel(c.provider)} · ${modelLabel(c.provider,c.model)}`;const prepared=await prepareAttachments(pendingFiles),persistentAttachment=prepared.metadata.some(f=>f.kind&&f.kind!=='midi')?prepared.modelText:'';c.messages.push({id:makeId(),role:"user",text:text.trim(),displayText:text,files:prepared.metadata,attachmentContext:persistentAttachment||undefined,createdAt:Date.now()});if(c.title==="Neuer Chat")c.title=text?text.slice(0,48):pendingFiles[0]?.name||"Datei-Chat";c.updatedAt=Date.now();saveChats();window.MCLUsageStartTurn?.({provider:c.provider,model:c.model,text});messageInput.value="";autoResize();pendingFiles=[];window.pendingFiles=pendingFiles;const thinking={id:makeId(),role:"assistant",provider:c.provider,model:c.model,text:`${providerLabel(c.provider)} antwortet …`,thinking:true,createdAt:Date.now()};c.messages.push(thinking);renderAll();try{c.messages=c.messages.filter(m=>m.id!==thinking.id);const result=await requestAssistant(c),usage=window.MCLUsageFinishTurn?.()||null,answer=result.visible||result.text||'';c.messages.push({id:makeId(),role:"assistant",provider:c.provider,model:c.model,text:answer,usage,mclAction:result.action||undefined,createdAt:Date.now()});window.dispatchEvent(new CustomEvent('mcl-request-result',{detail:{action:result.action||null,provider:c.provider,model:c.model}}))}catch(e){const usage=window.MCLUsageFinishTurn?.()||null;c.messages=c.messages.filter(m=>m.id!==thinking.id);c.messages.push({id:makeId(),role:"assistant",text:e?.message||String(e),usage,isError:true,createdAt:Date.now()})}finally{sending=false;c.updatedAt=Date.now();saveChats();renderAll()}}
function autoResize(){messageInput.style.height="auto";messageInput.style.height=`${Math.min(messageInput.scrollHeight,180)}px`}
providerSelect.addEventListener("change",()=>{updateModels();const c=currentChat();if(c){c.provider=providerSelect.value;c.model=modelSelect.value;c.updatedAt=Date.now();saveChats();renderChatList()}});modelSelect.addEventListener("change",()=>{const c=currentChat();if(c){c.model=modelSelect.value;c.updatedAt=Date.now();saveChats()}});fileInput.addEventListener("change",e=>{pendingFiles.push(...Array.from(e.target.files));window.pendingFiles=pendingFiles;fileInput.value="";renderPendingFiles()});settingsForm.addEventListener("submit",e=>{e.preventDefault();saveSettings({anthropicKey:anthropicKeyInput.value.trim(),openaiKey:openaiKeyInput.value.trim(),googleKey:googleKeyInput.value.trim()});composerNote.textContent="API-Einstellungen gespeichert.";settingsDialog.close()});clearKeysButton.addEventListener("click",()=>{localStorage.removeItem(SETTINGS_KEY);anthropicKeyInput.value="";openaiKeyInput.value="";googleKeyInput.value=""});settingsButton.addEventListener("click",openSettings);topSettingsButton.addEventListener("click",openSettings);newChatButton.addEventListener("click",createChat);menuButton.addEventListener("click",()=>sidebar.classList.toggle("open"));sendButton.addEventListener("click",sendMessage);messageInput.addEventListener("input",autoResize);messageInput.addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();sendMessage()}});if("serviceWorker"in navigator)window.addEventListener("load",()=>navigator.serviceWorker.register("service-worker.js"));renderAll();